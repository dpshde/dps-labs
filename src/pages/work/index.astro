---
import Layout from "../../layouts/Layout.astro";
import { education, experience, projects } from "../../data/site";
import type { Note } from "../../data/site";

const sections: { title: string; notes: Note[] }[] = [
  { title: "Selected", notes: projects },
  { title: "Experience", notes: experience },
  { title: "Education", notes: education },
];
---

<Layout
  context="work"
  title="Work · Dylan Shade"
  description="Selected tools and experience. Dylan Shade, software engineer and composer in Louisville."
>
  <h1>Work</h1>
  <p class="lede">
    Tools I’ve shipped, and the rooms I’ve worked in.
  </p>
  {
    sections.map((section) => (
      <section>
        <h2>{section.title}</h2>
        <ul class="note-list note-blocks">
          {section.notes.map((note) => (
            <li class="note">
              <div class="note-title">
                {note.href ? (
                  <a href={note.href} rel="noopener noreferrer" target="_blank">
                    {note.name}
                  </a>
                ) : (
                  <span>{note.name}</span>
                )}
                <span class="note-year">{note.year}</span>
              </div>
              <p>{note.description}</p>
            </li>
          ))}
        </ul>
      </section>
    ))
  }
</Layout>
